#include <stdio.h>
#include <stdlib.h>
#define _CRT_SECURE_NO_WARNNINGS

int strLen(char* str) {
	int i = 0;
	int count = 0;
	for (i = 0; str[i] != NULL; i++) {
		++count;
	}
	return count;
}
int main() {
	char* str="123456789";

	printf("%d \n", strLen(str));
	return 0;
}
